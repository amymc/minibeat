minibeat
========

Web app that calls the Chartbeat API and shows the top ten visited pages for gizmodo.com, sorted by current visitor number.

The list is updated every 5 seconds.

When an item in the list is clicked a list of its top referers is shown on the right.
